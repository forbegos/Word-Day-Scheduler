// Declarations

// add selectors here
var rowElement = $(".time-block");
console.log(rowElement);
console.log(rowElement.get(8).id);

// Declare object
var task = {
  time: "",
  description: "",
};

// Declare array of task
var taskArray = [];

// Populate Array

// for (let i = 0; i < 9; i++) {
// taskArray[i].task = //id from row 9
// taskArray[i].description = //text from row 9
// }
